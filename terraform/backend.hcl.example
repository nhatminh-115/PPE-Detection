# Copy this file to backend.hcl and fill in your values.
# backend.hcl is gitignored — never commit real values.
#
# Usage:
#   terraform init -backend-config=backend.hcl

bucket = "your-s3-bucket-name"
region = "us-east-1"
