# Copy to terraform.tfvars if you need to override any defaults.
# All defaults already match the production setup, so this file
# is only needed if you're deploying to a different environment.

# aws_region     = "us-east-1"
# project_name   = "sentinel-vision"
# environment    = "production"
# s3_bucket_name = "ppe-flywheel"
# s3_prefix      = "ppe-flywheel"
